# Blink Bot - MVP

Telegram-бот для сервиса междугороднего такси (аналог "Дайвинчика").

## Технологии

- Python 3.11+
- Aiogram 3.x
- PostgreSQL + asyncpg + SQLAlchemy 2.0
- Docker + Docker Compose

## Поддерживаемые языки

- 🇷🇺 Русский (ru)
- 🇰🇿 Қазақша (kz)
- 🇺🇿 O'zbekcha (uz)
- 🇬🇧 English (en)

## Установка и запуск

1. Скопируйте `.env.example` в `.env` и заполните переменные:
```bash
cp .env.example .env
```

2. Запустите через Docker Compose:
```bash
docker-compose up -d
```

3. Бот будет доступен в Telegram после запуска.

## Структура проекта

```
blinkbot/
├── bot.py                 # Точка входа
├── config.py              # Конфигурация
├── database/              # Работа с БД
│   ├── engine.py         # Настройка SQLAlchemy
│   ├── models.py         # Модели данных
│   └── requests.py       # Запросы к БД
├── handlers/              # Обработчики сообщений
│   └── user_handlers.py  # Обработчики пользователей
├── keyboards/             # Клавиатуры
│   └── inline.py         # Inline клавиатуры
├── locales/               # Локализация
│   ├── ru.py             # Русский
│   ├── kz.py             # Казахский
│   ├── uz.py             # Узбекский
│   ├── en.py             # Английский
│   └── translations.py   # Централизованная система переводов
├── middlewares/           # Middleware
│   └── i18n.py           # Middleware для интернационализации
├── states/                # FSM состояния
│   └── driver_states.py  # Состояния для водителя
├── docker-compose.yml     # Docker Compose конфигурация
├── Dockerfile            # Docker образ
└── requirements.txt      # Зависимости
```

## Основные функции

- Выбор языка при первом запуске
- Выбор роли (Водитель/Пассажир)
- Создание поездок (для водителей) - в разработке
- Поиск поездок (для пассажиров) - в разработке

## Разработка

Для локальной разработки без Docker:

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Настройте PostgreSQL локально или используйте Docker для БД:
```bash
docker-compose up db -d
```

3. Запустите бота:
```bash
python bot.py
```

